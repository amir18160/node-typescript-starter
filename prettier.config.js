const config = {
    singleQuote: false, // Use double quotes instead of single quotes
    printWidth: 80, // Wrap lines at 90 characters
    tabWidth: 4, // Use 4 spaces for indentation
    sami: true,
    arrowParens: "always",
};

module.exports = config;
